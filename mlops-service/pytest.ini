# Pytest Configuration (OPTIONAL - tests work without this)
# Lab 3: Testing AI Systems
# This file makes testing output cleaner but is not required

[tool:pytest]
addopts = -v --tb=short --color=yes
testpaths = .
python_files = test_*.py